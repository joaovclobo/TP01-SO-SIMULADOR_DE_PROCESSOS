#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int MenuInicial(FILE** arquivoDeEntrada);
int MenuImpressaoProcesso();
FILE* abreArquivoRead(char* nomeArquivo);

void flag(int i);
